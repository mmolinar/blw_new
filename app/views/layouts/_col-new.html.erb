<!--</% @posts.each do |post| %>
<!-- <li>
    <!------------ 1ST ARTICLE ------------
    <div class="row article-tumbnail-top">
        <div class="col-md-6 article-thumbnail">
            <!------- IMAGE -------
            <a href="#"></%= image_tag post.pic.url(:medium), class: "img-thumbnail responsive" %></a>
        </div>
        <!------- END IMAGE -------
        <div class="col-md-6 article-text">
            <!------- ARTICLE -------
            <h3 class="category-title">
                <a href="#">1st article</a>
            </h3>
            <a class="column-article-title" href="#">
                <h3 class="article-title"><.%= link_to post.title, post%></h3>
                <p class="article-author"><small>
                    </%= post.created_at.strftime("%d %B, %Y") %></small>
                </p>
                <p>
                    </%= link_to post.description.truncate_words(35), post%>
                </p>
            </a>
            
            <div class="article-author">
                
            </div>
        </div>
        <!------- END ARTICLE -------
    </div>
</li>
</% end %>
<!------------ END 1ST ARTICLE ------------>

<% @posts.each do |post| %>
  <div class="col-lg-4">
    <a href="#">
      <%= image_tag post.pic.url, class: "posts-pic img-fluid responsive card-img-top" %>
    </a>
    <h4 class="card-title"><%= link_to post.title, post%></h4>
    <p class="card-text"><%= link_to post.description.truncate_words(35), post%></p>
    <p class="card-text"><small class="text-muted"><%= post.created_at.strftime("%d %B, %Y") %></small></p>
  </div>
  <% end %>